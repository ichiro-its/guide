.. _pip-guide:

Manage Python Packages Using pip
================================

`pip <https://pip.pypa.io/en/stable/>`_ is a package-management system written in :ref:`Python <python-guide>` used to install and manage software packages.
It connects to an online repository of public packages, called the `Python Package Index <https://pypi.org/>`_ (PyPI).
